from setuptools import setup

setup(
    name='disk',
    version='0.0.1',
    description='Project DISK with adaptation for 3DGS in PyTorch code',
    packages=['disk', 'disk.common', 'disk.data', 'disk.geom', 'disk.loss', 'disk.model'],
    author='Fan WU',
    author_email='fanwuchine@gmail.com',
)
